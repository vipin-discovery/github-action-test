name: Github Action

on: [push]

jobs:
  run-github-action:
    runs-on: ubuntu-latest
    steps:
      - name: Simple JS Action
        id: greet
        uses: action/hello-world-javascript-actions@be24b1ba147bc3acbf8f3f3665b3cca7e4f60a76
        with:
          who-to-greet: Vipin
      - name: Log Greeting Time
        run: echo "${{ steps.greet.outputs.time }}"    